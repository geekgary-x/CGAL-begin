#include <gtest/gtest.h>
#include <Series/index.h>
TEST(CGALBEGIN, predicates_and_constructions)
{
	predicates_and_constructions();
}

TEST(CGALBEGIN, surprising)
{
	surprising();
}

TEST(CGALBEGIN, miniqtproject)
{
	miniqtproject();
}